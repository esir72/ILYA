---

---

<section class="hero">
  <div class="container hero-inner">
    <div class="hero-content">
      <p class="hero-label" data-animate="fade" data-delay="1" data-i18n="hero.label">Мастер резьбы по дереву</p>
      <h1 class="hero-title" data-animate data-delay="2" data-i18n="hero.title">Илья</h1>
      <p class="hero-desc" data-animate data-delay="3" data-i18n="hero.desc">
        Создаю уникальные изделия из натурального дерева. Каждая работа — 
        это сочетание традиционного мастерства и современного дизайна.
      </p>
    </div>
    <div class="hero-nav" data-animate data-delay="4">
      <a href="#products" class="hero-card">
        <div class="hero-card-img" style="background-image: url('/images/sumka-iz-dereva.jpg')">
          <span class="hero-card-label" data-i18n="hero.cardProducts">Изделия</span>
        </div>
      </a>
      <a href="#tools" class="hero-card">
        <div class="hero-card-img" style="background-image: url('/images/10 Mind-Blowing Wood Carving Techniques You Need To Try Today! - The Table Read Magazine.jpg')">
          <span class="hero-card-label" data-i18n="hero.cardTools">Инструменты</span>
        </div>
      </a>
    </div>
  </div>
</section>

<style>
  .hero {
    min-height: 100vh;
    display: flex;
    align-items: center;
    padding-top: 64px;
    background: linear-gradient(180deg, var(--bg) 0%, var(--beige) 100%);
  }

  .hero-inner {
    display: grid;
    grid-template-columns: 2fr 3fr;
    gap: 60px;
    align-items: center;
  }

  .hero-content {
    max-width: 540px;
  }

  .hero-label {
    font-size: 0.9rem;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    color: var(--accent);
    margin-bottom: 16px;
  }

  .hero-title {
    font-size: 4rem;
    margin-bottom: 24px;
    color: var(--text);
  }

  .hero-desc {
    font-size: 1.15rem;
    color: var(--text-light);
    line-height: 1.7;
    margin-bottom: 40px;
  }

  .hero-nav {
    display: flex;
    gap: 24px;
  }

  .hero-card {
    text-decoration: none;
    transition: transform 0.3s, box-shadow 0.3s;
    flex: 1;
  }

  .hero-card:hover {
    transform: translateY(-4px);
  }

  .hero-card-img {
    width: 100%;
    aspect-ratio: 4 / 3;
    background-size: cover;
    background-position: center;
    border: 1px solid var(--border);
    display: flex;
    align-items: center;
    justify-content: center;
    position: relative;
    transition: border-color 0.3s;
  }

  .hero-card-img::before {
    content: '';
    position: absolute;
    inset: 0;
    background: rgba(0,0,0,0.4);
    transition: background 0.3s;
  }

  .hero-card:hover .hero-card-img::before {
    background: rgba(0,0,0,0.25);
  }

  .hero-card-label {
    position: relative;
    z-index: 1;
    font-family: var(--font-heading);
    font-size: 2rem;
    color: #fff;
    text-transform: uppercase;
    letter-spacing: 0.08em;
    text-shadow: 0 2px 8px rgba(0,0,0,0.5);
  }

  @media (max-width: 768px) {
    .hero-inner {
      grid-template-columns: 1fr;
      text-align: center;
    }
    .hero-content {
      margin: 0 auto;
    }
    .hero-title {
      font-size: 3rem;
    }
    .hero-nav {
      flex-direction: row;
      justify-content: center;
      gap: 16px;
    }
    .hero-card {
      flex: 1;
      max-width: 260px;
    }
    .hero-card-img {
      aspect-ratio: 4 / 3;
    }
    .hero-card-label {
      font-size: 1rem;
    }
  }

  @media (max-width: 480px) {
    .hero-title {
      font-size: 2.2rem;
    }
    .hero-desc {
      font-size: 1rem;
      margin-bottom: 24px;
    }
    .hero-nav {
      flex-direction: column;
      align-items: center;
    }
    .hero-card {
      flex: none;
      width: 100%;
      max-width: 260px;
    }
    .hero-card-img {
      aspect-ratio: 4 / 3;
    }
  }
</style>
